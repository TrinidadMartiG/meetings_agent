# Railway — set these in Railway dashboard (not committed)
GEMINI_API_KEY=
GOOGLE_CLIENT_ID=
JWT_SECRET=           # openssl rand -hex 32
ALLOWED_ORIGINS=      # URL del frontend Vercel: https://kam.vercel.app

# Vercel — set these in Vercel dashboard (not committed)
NEXTAUTH_URL=         # URL del frontend: https://kam.vercel.app
NEXTAUTH_SECRET=      # openssl rand -hex 32
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
NEXT_PUBLIC_API_URL=  # URL del backend Railway: https://backend.railway.app
